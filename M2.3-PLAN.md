# M2.3: Client-Side Prediction & Reconciliation

**Приоритет:** 🔴 **КРИТИЧНО**  
**Статус:** Не начат  
**Зависимости:** M2.2 Server (✅ Готово)  
**Время:** 2-3 недели (оптимистично), 3-4 недели (реалистично)

---

## Исполнительное резюме

M2.2 реализовал **серверную часть** UDP транспорта. Для полноценного multiplayer необходима **клиентская часть**:

- ❌ TypeScript/JavaScript клиент
- ❌ Client-side prediction (локальная физика)
- ❌ Reconciliation/replay (синхронизация с сервером)
- ❌ DoD verification (2 игрока, RTT 50/200 ms)

**Без M2.3 невозможно:**

- Завершить M2.2 DoD
- Тестировать M3 (FA:ON/OFF) онлайн
- Двигаться к играбельному прототипу

---

## Цель

Реализовать клиентскую часть сетевой инфраструктуры для **responsive multiplayer gameplay** с компенсацией сетевых задержек.

---

## Definition of Done (DoD)

### Функциональные требования

- ✅ TypeScript клиент подключается к серверу (WebSocket/UDP)
- ✅ Локальная физика идентична серверной (детерминизм)
- ✅ Client-side prediction применяет inputs немедленно
- ✅ Reconciliation корректирует расхождения с сервером
- ✅ 30 Hz input rate (контроль частоты отправки)

### Качество сети (DoD из spec)

- ✅ **2 игрока онлайн** работает стабильно
- ✅ **RTT 50 ms**: prediction error < 1 метр (среднее)
- ✅ **RTT 200 ms**: конвергенция с сервером < 2 секунды

### Тестирование

- ✅ Интеграционные тесты (2 игрока)
- ✅ Сетевые тесты (RTT 50/200 ms)
- ✅ Unit-тесты для prediction/reconciliation

---

## Архитектура

### 1. TypeScript Client (Browser-Based)

```
src/client/
  network/
    WebSocketClient.ts      # Transport layer
    MessageHandler.ts       # Protobuf routing
    ConnectionManager.ts    # Connection state
  
  physics/
    LocalPhysics.ts         # Client-side physics (порт с C#)
    PredictionManager.ts    # Input buffering + prediction
    ReconciliationManager.ts # Server sync + replay
  
  rendering/
    Renderer.ts             # Canvas 2D rendering
    Camera.ts               # Viewport
  
  input/
    InputManager.ts         # WASD/Mouse
  
  game/
    GameClient.ts           # Main client loop (60 FPS)
```

### 2. Client-Side Prediction

**Принцип:**

1. Клиент немедленно применяет input локально (0 ms задержка)
2. Input сохраняется в history buffer с sequence number
3. Input отправляется на сервер (30 Hz)
4. Локальная физика выполняет шаг (идентично серверу)

**Результат:** Мгновенный отклик, несмотря на network lag.

### 3. Reconciliation (Replay)

**Принцип:**

1. Клиент получает server snapshot (15 Hz)
2. Сравнивает свою predicted position с server position
3. Если расхождение > tolerance (0.5м):
   - Rewind: откат к server state
   - Replay: повторное применение inputs после последнего обработанного сервером
4. Если расхождение < tolerance: игнорируем (мелкая ошибка)

**Результат:** Плавная синхронизация с сервером без "прыжков".

---

## Изменения в серверном коде

### 1. Добавить `last_processed_input` в протокол

```protobuf
// В game.proto
message EntitySnapshot {
  // ... существующие поля ...
  uint32 last_processed_input = 6;  // НОВОЕ
}
```

### 2. Сохранять sequence number в ConnectionManager

```csharp
// В ClientConnection.cs
public class ClientConnection {
  // ... существующие поля ...
  public uint LastProcessedSequence { get; set; }  // НОВОЕ
}
```

### 3. Обновить MessageProcessor

```csharp
// В MessageProcessor.cs
private void HandlePlayerInput(PlayerInputProto input, IPEndPoint endpoint) {
  // ... существующий код ...
  
  // НОВОЕ: сохранить последний обработанный sequence
  connection.LastProcessedSequence = input.SequenceNumber;
}
```

### 4. Включить lastProcessedInput в snapshot

```csharp
// В NetworkGameLoop.cs
private async Task BroadcastWorldSnapshot() {
  // ... существующий код ...
  
  foreach (var entity in entities) {
    var snapshot = EntitySerializer.ToSnapshot(entity);
    
    // НОВОЕ: добавить lastProcessedInput
    var connection = _connectionManager.GetClientByEntityId(entity.id);
    if (connection != null) {
      snapshot.LastProcessedInput = connection.LastProcessedSequence;
    }
    
    worldSnapshot.Entities.Add(snapshot);
  }
}
```

---

## План реализации (пошагово)

### Фаза 1: Каркас клиента (3-5 дней)

- [ ] TypeScript проект (Vite уже есть)
- [ ] WebSocket client (подключение к серверу)
- [ ] Protobuf integration (protobuf.js)
- [ ] Базовый рендеринг (Canvas 2D, отображение кораблей)
- [ ] Input manager (WASD/мышь)

**Milestone:** Клиент подключается и отображает корабли из server snapshots.

---

### Фаза 2: Локальная физика (3-5 дней)

- [ ] Портировать PhysicsSystem.cs → LocalPhysics.ts
- [ ] Портировать RelativisticMath → TypeScript
- [ ] Портировать Vector2/Transform/Velocity → TypeScript
- [ ] Unit-тесты: сравнение C# vs TypeScript результатов

**Milestone:** Локальная физика даёт те же результаты, что серверная.

---

### Фаза 3: Client-side prediction (2-3 дня)

- [ ] Input history buffer
- [ ] Sequence number generation
- [ ] Применение input локально
- [ ] Отправка на сервер (30 Hz)
- [ ] Game loop (60 FPS)

**Milestone:** Корабль откликается мгновенно на input.

---

### Фаза 4: Reconciliation (3-5 дней)

- [ ] Обновить protobuf (last_processed_input)
- [ ] Обновить сервер (сохранять sequence)
- [ ] Reconciliation manager (rewind + replay)
- [ ] Tolerance threshold (игнорировать малые ошибки)
- [ ] Тесты: convergence time

**Milestone:** Расхождения с сервером корректируются плавно.

---

### Фаза 5: DoD Verification (2-3 дня)

- [ ] Интеграционный тест: 2 игрока онлайн
- [ ] Сетевой тест: RTT 50 ms (prediction error < 1м)
- [ ] Сетевой тест: RTT 200 ms (convergence < 2 сек)
- [ ] Manual testing: визуальная проверка

**Milestone:** Все DoD критерии выполнены.

---

## Критические риски

### Риск 1: Детерминизм физики (C# vs TypeScript)

**Проблема:** Floating-point математика может различаться.

**Решение:**

- Фиксированный deltaTime (1/60 сек)
- Избегать platform-specific функций (Math.sin/cos одинаковы)
- Автоматические тесты: same inputs → same outputs
- Property-based тесты для проверки идентичности

### Риск 2: "Прыжки" при reconciliation

**Проблема:** Rewind вызывает резкое перемещение корабля.

**Решение:**

- Tolerance threshold (не корректировать малые ошибки < 0.5м)
- Smooth interpolation (опционально, если прыжки всё равно заметны)
- Adaptive replay (только при больших расхождениях)

### Риск 3: Производительность клиента

**Проблема:** 60 FPS + physics + replay может быть медленным.

**Решение:**

- Профилирование (Chrome DevTools)
- Оптимизация physics (SIMD, если доступен)
- Spatial culling (не симулировать далёкие корабли)

---

## Метрики успеха

### Качество prediction

- **Prediction error (RTT 50ms):** < 1 метр (среднее)
- **Prediction error (RTT 200ms):** < 5 метров (среднее)
- **Convergence time (RTT 200ms):** < 2 секунды

### Производительность

- **Client FPS:** 60 (stable)
- **Input latency:** < 16 ms (один frame @ 60 FPS)
- **Network usage:** < 10 KB/s (per client)

### Стабильность

- **Packet loss tolerance:** 5% (graceful degradation)
- **Connection timeout:** 10 секунд без snapshots

---

## Следующий шаг после M2.3

**M3: FA:ON/OFF и Stabilized Ассистент** (3-4 недели)

Требует M2.3 для тестирования онлайн, но можно начать параллельно (серверная часть FA:ON/OFF независима от клиента).

---

**Документ создан:** 2025-11-18  
**Автор:** AI Assistant  
**Статус:** ✅ Ready to start  
**Следующее действие:** Создать issue/branch для M2.3
