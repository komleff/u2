# Итоговое резюме (перевод)

## Summary (Русский перевод)

- учтено настраиваемое пороговое значение реконсиляции при инициализации движка предсказаний
- добавлено глубокое копирование состояний сущностей во время предсказания и реконсиляции, чтобы избежать общих мутаций
- добавлены целевые тесты, проверяющие работу реконсиляции с настраиваемыми порогами

## Testing

- npm run lint
- npx vitest run tests/prediction.spec.ts
- npm test *(падает: интеграционные наборы требуют dotnet, которого нет в окружении)*
