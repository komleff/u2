# Аудит проекта Universe Unlimited v0.8.6

**Дата аудита:** 15 ноября 2025  
**Версия документации:** 0.8.6  
**Аудитор:** Claude (Anthropic)  
**Целевая аудитория:** Гейм-дизайнер/продюсер + ИИ-агенты

---

## Резюме

**Общая оценка:** ⭐⭐⭐⭐ (4/5) — Хорошо структурированный проект с правильными приоритетами

**Основной вывод:** Документация хорошо подготовлена для работы с ИИ-агентами. Технологический стек обоснован, архитектура упрощена до разумного минимума, план разработки реалистичен. Выявлено несколько критических рисков, требующих внимания.

**Ключевые сильные стороны:**
- ✅ Четкая этапность и критерии готовности
- ✅ Правильное упрощение скоупа (один режим ассистента, фиксированный c′)
- ✅ Единая система единиц (СИ) для всех компонентов
- ✅ Разделение ответственности между клиентом и сервером

**Критические риски:**
- ⚠️ Отсутствие прототипирования сетевого кода до M2
- ⚠️ Зависимость от Entitas без проверки совместимости с архитектурой
- ⚠️ Недостаточная детализация интерфейсов для ИИ-агентов
- ⚠️ Оптимистичные сроки без буферов на интеграцию

---

## 1. Анализ технологического стека

### 1.1. Клиентская часть (Unity)

#### Сильные стороны
- **3D-движок для 2D-игры обоснован:** Правильное решение для дальности обзора 10-20 км, решает проблемы depth-буфера
- **URP вместо HDRP:** Адекватный выбор для кроссплатформенности (ПК, WebGL, мобильные)
- **Двухкамерная схема:** Элегантное решение для показа близких и далёких объектов

#### Риски и рекомендации

**🔴 КРИТИЧНО: Отсутствие контрольных точек производительности**
```
Проблема: В документации нет конкретных метрик производительности
Рекомендация: Добавить на M0:
- Целевой FPS: 60 на desktop, 30 на mobile
- Максимальное количество объектов в сцене: 200
- Бюджет drawcalls: 150-200
- Память текстур: <200 MB на desktop, <100 MB на mobile
```

**🟡 ВАЖНО: Addressables добавлены слишком рано**
```
Проблема: В M0-M8 не планируется динамическая загрузка контента
Рекомендация: Отложить Addressables до M9+, использовать Resources на ранних этапах
Экономия: ~1-2 недели разработки
```

**🟢 ХОРОШО: Input System**
```
Правильный выбор для мультиплатформенности
Дополнение: Добавить в M0 валидацию работы на геймпаде, клавиатуре и тач
```

### 1.2. Серверная часть (.NET 8)

#### Сильные стороны
- **Общий язык (C#)** с клиентом упрощает разработку и шаринг кода
- **.NET 8** — стабильная LTS-версия с хорошей производительностью
- **Headless server** — правильный выбор для серверной архитектуры

#### Риски и рекомендации

**🔴 КРИТИЧНО: Двойной сетевой стек (UDP + WebSocket)**
```
Проблема: Поддержка двух транспортов сразу увеличивает сложность в 1.5-2 раза
Рекомендация:
M0-M5: Только UDP для desktop-клиентов
M6-M8: Добавление WebSocket для WebGL
Экономия: ~3-4 недели разработки + снижение риска багов
```

**🟡 ВАЖНО: LiteNetLib vs встроенный транспорт Unity**
```
Проблема: Использование LiteNetLib требует ручной интеграции
Альтернатива: Netcode for GameObjects или Mirror
Оценка: LiteNetLib оправдан для performance-critical случаев, но требует больше ручного кода
Рекомендация: Сохранить LiteNetLib, но добавить обёртку для упрощения работы ИИ-агентов
```

**🟢 ХОРОШО: Protobuf для сериализации**
```
Компактность и версионирование — правильный выбор
Дополнение: Генерировать C# классы из .proto файлов на этапе сборки
```

### 1.3. ECS-архитектура (Entitas)

#### Критический анализ

**🔴 КРИТИЧНО: Отсутствие прототипа на Entitas**
```
Проблема: Документация предполагает Entitas, но нет доказательств совместимости с архитектурой
Риск: Может оказаться, что Entitas не подходит для синхронизации клиент-сервер

Рекомендация:
M0 (неделя 1-2): Spike solution
- Создать минимальный прототип: 1 корабль, движение, репликация клиент-сервер
- Проверить совместимость Entitas с авторитетным сервером
- Оценить overhead на serialization компонентов
- Если не подходит, переключиться на базовый OOP или создать собственную минималистичную ECS

Альтернатива: Собственная minimal ECS (2-3 недели на M0)
- Компоненты: Position, Velocity, Ship, Health
- Системы: Movement, Combat, Networking
- Без генерации кода, полный контроль
```

**Сравнение вариантов ECS:**

| Вариант | Сложность | Контроль | Подходит для ИИ | Риск |
|---------|-----------|----------|----------------|------|
| Entitas | Средняя | Средний | ⚠️ Требует понимания кодогенерации | Средний |
| Unity DOTS | Высокая | Низкий | ❌ Слишком сложно для ИИ | Высокий |
| Собственная ECS | Низкая | Высокий | ✅ Простой код, легко модифицировать | Низкий |
| Чистый OOP | Низкая | Высокий | ✅ Очень понятно | Низкий, но ограничена масштабируемость |

**Рекомендация для ИИ-агентов:** Собственная minimal ECS или чистый OOP до M5, затем оценить необходимость Entitas

---

## 2. Анализ архитектуры

### 2.1. Физическая модель

#### Сильные стороны
- **2D физика при 3D рендере:** Упрощает расчёты без потери визуального качества
- **Фиксированный c′ в локации:** Правильное упрощение, избегает сложности динамического изменения
- **Релятивистские эффекты через γ:** Элегантное решение для "скоростной игры"

#### Риски и рекомендации

**🟡 ВАЖНО: Точность вычислений**
```
Проблема: Сервер использует double, клиент float
Риск: Расхождения могут накапливаться и приводить к заметным рассинхронизациям

Рекомендация:
M1: Добавить автотесты на проверку накопления ошибок
- Симуляция 10 минут полёта
- Максимальное расхождение позиции: <5 метров
- Максимальное расхождение скорости: <0.5 м/с

M2: Implement reconciliation с толерантностью:
- Позиция: ±2 метра
- Скорость: ±0.2 м/с
```

**🟢 ХОРОШО: Система единиц (СИ)**
```
Единообразие упрощает отладку и работу ИИ-агентов
Дополнение: Создать helper-функции для конвертации (м/с -> км/ч для UI)
```

### 2.2. Сетевая модель

#### Сильные стороны
- **Авторитетный сервер:** Правильный выбор для защиты от читерства
- **Client-side prediction + reconciliation:** Стандартный подход для action-игр
- **Очередь будущих состояний:** Элегантное решение для маскировки лага

#### Риски и рекомендации

**🔴 КРИТИЧНО: Reconciliation недостаточно детализирован**
```
Проблема: В документации нет алгоритма reconciliation
Риск: ИИ-агент может реализовать неправильно, что приведет к "телепортам"

Рекомендация для M2: Конкретизировать алгоритм
Псевдокод:
```python
def reconcile(client_state, server_snapshot):
    # 1. Найти соответствующий клиентский стейт по server_tick
    client_tick = find_client_state(server_snapshot.tick)
    
    # 2. Вычислить рассинхронизацию
    position_error = server_snapshot.position - client_tick.position
    velocity_error = server_snapshot.velocity - client_tick.velocity
    
    # 3. Применить smoothing
    if position_error.magnitude < TELEPORT_THRESHOLD:  # 5 метров
        # Плавная коррекция за 200мс
        apply_smooth_correction(position_error, 0.2)
    else:
        # Резкая телепортация
        snap_to_server_position(server_snapshot.position)
    
    # 4. Пересимулировать все тики после server_tick
    replay_inputs_from(server_snapshot.tick + 1)
```
```

**🟡 ВАЖНО: Частоты обновлений**
```
Заявлено:
- Физика: 30 Hz
- Сеть от сервера: 15 Hz
- Ввод к серверу: 30 Hz
- Рендер: 60 Hz

Проблема: Разные частоты создают сложность для ИИ-агентов

Рекомендация:
M0: Создать диаграмму timing для визуализации
M1: Добавить debug-режим с фиксированными 30 Hz на всех уровнях для упрощения отладки
```

**🟢 ХОРОШО: Асинхронные уровни обновления**
```
30 Hz: Физика, управление
15 Hz: Нагрев, охлаждение
10 Hz: ИИ

Правильное разделение по критичности
```

### 2.3. Масштаб и нагрузка

#### Оценка адекватности

**🟢 ХОРОШО: Консервативные цифры**
```
4 игрока + 8 ботов = 12 кораблей
Оценка: Адекватно для прототипа

Расчет нагрузки на сервер (грубая оценка):
- 12 кораблей × 30 Hz = 360 обновлений физики/сек
- 12 кораблей × 15 Hz = 180 обновлений термодинамики/сек
- 8 ботов × 10 Hz = 80 решений ИИ/сек
- Коллизии: O(n²) = 144 проверок при naive подходе, ~12 при spatial partitioning

Вердикт: Одноядерный сервер на .NET справится с запасом
```

**Рекомендация:** Добавить в M0 benchmark с 20 кораблями для проверки запаса производительности

---

## 3. Анализ плана разработки

### 3.1. Этапность и последовательность

#### Структура этапов

**🟢 ХОРОШО: Логичная последовательность**
```
M0: База → M1: Физика → M2: Сеть → M3: Управление → M4: HUD → M5: Аварийки → M6: Бой → M7: Термодинамика → M8: Оптимизация
```

**🔴 КРИТИЧНО: M2 слишком рано**
```
Проблема: Сетевой код внедряется до полной отладки локальной симуляции
Риск: Сложно отлаживать физику + сеть одновременно

Рекомендация: Изменить последовательность
M0: Инфраструктура + ECS spike
M1: Физика + офлайн-клиент + расширенное тестирование (2 корабля локально)
M1.5: (НОВЫЙ ЭТАП, 2 недели) Стабилизация физики
  - Stress-тесты: 1000 тиков подряд без взрывов
  - Проверка инвариантов
  - Документирование численных параметров
M2: Сеть (с уже стабильной физикой)
```

### 3.2. Оценка сроков

#### Общая оценка: **24-30 недель**

**Разбивка по реалистичности:**

| Этап | Заявлено | Реалистично | Комментарий |
|------|----------|-------------|-------------|
| M0 | 10 недель | **12-14 недель** | + spike на Entitas, + setup CI/CD |
| M1-M3 | 10 недель | **14-16 недель** | + стабилизация физики, + отладка reconciliation |
| M4-M6 | 10 недель | **10-12 недель** | Оценка адекватна при условии стабильного фундамента |
| M7-M8 | 4-6 недель | **6-8 недель** | Оптимизация всегда занимает больше времени |
| **ИТОГО** | **24-30** | **32-42 недели** | **8-10 месяцев** |

**🔴 КРИТИЧНО: Отсутствие буферов на интеграцию**
```
Проблема: План не учитывает время на исправление багов между этапами
Рекомендация: Добавить integration sprints по 1 неделе после M3, M6, M8
Итого: +3 недели → 35-45 недель (9-11 месяцев)
```

### 3.3. Критерии готовности

**🟡 ВАЖНО: Недостаточно конкретики**
```
Пример размытого критерия (M2):
"Среднее расхождение позиции — менее метра"

Проблема: Как измерять "среднее"? За какой период?

Улучшенный критерий:
"При RTT=100мс и стабильном соединении:
- 95-й перцентиль расхождения позиции < 1 метр
- 99-й перцентиль расхождения позиции < 3 метра
- Измерение на интервале 60 секунд полёта
- Минимум 10 тестовых прогонов"
```

**Рекомендация:** Переписать все критерии готовности в формате SMART

---

## 4. Адаптация для ИИ-агентов

### 4.1. Что хорошо для ИИ

**✅ Сильные стороны документации:**

1. **Единая система единиц** — ИИ не запутается в конвертациях
2. **Четкое разделение клиент-сервер** — явные границы ответственности
3. **Вынос ТТХ в отдельный документ** — data-driven approach понятен ИИ
4. **Явные частоты обновлений** — нет неопределённости

**❌ Проблемные места для ИИ:**

1. **Отсутствие примеров кода** — ИИ будет делать assumptions
2. **Размытые интерфейсы** — нет контрактов между компонентами
3. **Неявные зависимости** — ИИ может создать циклические зависимости
4. **Отсутствие архитектурных диаграмм** — сложно понять общую картину

### 4.2. Рекомендации для работы с ИИ-агентами

#### **Создать "ИИ-дружественные" артефакты**

**1. Интерфейсы как контракты (для M0)**
```csharp
// Примеры интерфейсов для ИИ-агентов
namespace U2.Core.Physics
{
    /// <summary>
    /// Физическое состояние объекта в мире
    /// ИИ: Все координаты в метрах (СИ), скорости в м/с
    /// </summary>
    public interface IPhysicsState
    {
        /// <summary>Position in meters</summary>
        Vector2d Position { get; }
        
        /// <summary>Velocity in m/s</summary>
        Vector2d Velocity { get; }
        
        /// <summary>Orientation in radians</summary>
        double Orientation { get; }
        
        /// <summary>Angular velocity in rad/s</summary>
        double AngularVelocity { get; }
    }
    
    /// <summary>
    /// Физический движок
    /// ИИ: Реализовать для сервера (double) и клиента (float)
    /// </summary>
    public interface IPhysicsEngine
    {
        /// <summary>
        /// Обновить физику на один фиксированный тик (1/30 сек)
        /// ИИ: ВАЖНО - dt всегда = 0.0333... секунды
        /// </summary>
        void Tick(double deltaTime);
        
        /// <summary>
        /// Добавить силу к кораблю
        /// </summary>
        /// <param name="shipId">ID корабля</param>
        /// <param name="force">Сила в ньютонах</param>
        void ApplyForce(int shipId, Vector2d force);
    }
}
```

**2. Архитектурные диаграммы (для M0)**

```
Диаграмма компонентов:

┌─────────────────────────────────────────────────────────┐
│                    CLIENT (Unity)                       │
│                                                         │
│  ┌────────────┐    ┌──────────────┐   ┌─────────────┐ │
│  │  Renderer  │◄───│ Presentation │◄──│ State Store │ │
│  └────────────┘    └──────────────┘   └──────┬──────┘ │
│                                               │        │
│  ┌────────────┐    ┌──────────────┐          │        │
│  │Input System│───►│  Controller  │──────────┘        │
│  └────────────┘    └──────────────┘                   │
│                                                         │
│  ┌────────────────────────────────────────────────┐   │
│  │     Client-Side Prediction (float)            │   │
│  │  - Предсказание своего корабля                 │   │
│  │  - Очередь будущих состояний для других        │   │
│  └────────────────────────────────────────────────┘   │
│                           ▲                            │
│                           │ Protobuf Messages          │
│                           │ (15 Hz snapshots)          │
└───────────────────────────┼────────────────────────────┘
                            │
                   ┌────────┴────────┐
                   │ UDP / WebSocket │
                   └────────┬────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│                   SERVER (.NET 8)                       │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │         Authoritative Simulation (double)        │  │
│  │  ┌────────────┐  ┌──────────┐  ┌─────────────┐  │  │
│  │  │  Physics   │  │  Combat  │  │   Thermal   │  │  │
│  │  │  (30 Hz)   │  │ (30 Hz)  │  │   (15 Hz)   │  │  │
│  │  └────────────┘  └──────────┘  └─────────────┘  │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │              AI & Automation (10 Hz)             │  │
│  │  - Bot decision making                           │  │
│  │  - Autopilot                                     │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │                  ECS Storage                     │  │
│  │  Components: Position, Velocity, Ship,           │  │
│  │              Health, Shield, etc.                │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘

ИИ: Заметки по диаграмме:
- Клиент НИКОГДА не симулирует других игроков (только своего)
- Сервер - единственный источник истины
- Reconciliation происходит когда сервер корректирует клиента
```

**3. Decision Trees для неоднозначных ситуаций (для M1)**

```
Что делать при конфликте ввода от разных источников?

START
  │
  ▼
┌─────────────────────┐
│ Получена команда    │
│ управления          │
└──────────┬──────────┘
           │
           ▼
      ┌─────────┐
      │ Local?  │──YES──► ПРИОРИТЕТ 1: Немедленно применить
      └────┬────┘
           │ NO
           ▼
      ┌─────────┐
      │ Remote? │──YES──► ПРИОРИТЕТ 2: Проверить, не активен ли Local
      └────┬────┘         │
           │ NO           ├──Local активен──► Игнорировать Remote
           ▼              └──Local неактивен─► Применить Remote
     ┌───────────┐
     │Autopilot? │──YES──► ПРИОРИТЕТ 3: Применить если Local и Remote неактивны
     └─────┬─────┘         за последние 200мс
           │ NO
           ▼
     ┌─────────┐
     │ Script? │──YES──► ПРИОРИТЕТ 4: Применить только в тестовом режиме
     └─────────┘

ИИ: Реализовать как state machine в PilotAssistant.cs
```

**4. Тестовые кейсы как спецификация (для каждого этапа)**

```csharp
// M1_Physics_Tests.cs
// ИИ: Эти тесты ДОЛЖНЫ проходить для завершения M1

[TestFixture]
public class M1_Physics_Acceptance_Tests
{
    [Test]
    public void Physics_Should_Not_Explode_Over_1000_Ticks()
    {
        // Arrange
        var engine = new PhysicsEngine();
        var ship = CreateTestShip(mass: 10000); // 10 тонн
        engine.AddShip(ship);
        
        // Act
        for (int i = 0; i < 1000; i++)
        {
            engine.Tick(1.0 / 30.0);
        }
        
        // Assert
        var state = engine.GetShipState(ship.Id);
        Assert.That(state.Position.Magnitude, Is.LessThan(100000)); // <100 км
        Assert.That(state.Velocity.Magnitude, Is.LessThan(10000)); // <10 км/с
        Assert.That(double.IsNaN(state.Position.X), Is.False);
        Assert.That(double.IsInfinity(state.Velocity.Y), Is.False);
    }
    
    [Test]
    public void Ship_Should_Not_Exceed_Speed_Limit()
    {
        // ИИ: c' = 5000 м/с, максимум 0.9c = 4500 м/с
        var engine = new PhysicsEngine(lightSpeed: 5000);
        var ship = CreateTestShip(maxThrust: 1_000_000); // 1 МН
        
        // Постоянно применяем максимальную тягу
        for (int i = 0; i < 10000; i++)
        {
            engine.ApplyForce(ship.Id, new Vector2d(1_000_000, 0));
            engine.Tick(1.0 / 30.0);
        }
        
        var state = engine.GetShipState(ship.Id);
        Assert.That(state.Velocity.Magnitude, Is.LessThan(4500));
    }
}
```

**5. Глоссарий с примерами для ИИ (M0)**

```markdown
## Глоссарий для ИИ-агентов

### Reconciliation (согласование)
**Человеческое объяснение:** Процесс исправления клиентского предсказания на основе данных сервера

**Пример для ИИ:**
```csharp
// НЕПРАВИЛЬНО - резкая телепортация
void Reconcile(Vector2 serverPos)
{
    transform.position = serverPos; // Игрок увидит прыжок!
}

// ПРАВИЛЬНО - плавная коррекция
void Reconcile(Vector2 serverPos)
{
    Vector2 error = serverPos - predictedPosition;
    if (error.magnitude < 5.0f) // Порог телепортации
    {
        // Применяем коррекцию за 0.2 секунды
        StartCoroutine(SmoothCorrection(error, 0.2f));
    }
    else
    {
        // Слишком большая ошибка - телепортируем
        transform.position = serverPos;
    }
}
```

### γ (гамма-фактор)
**Формула:** γ = 1 / sqrt(1 - (v²/c'²))

**Пример для ИИ:**
```csharp
double CalculateGamma(double velocity, double lightSpeed)
{
    double beta = velocity / lightSpeed;
    double betaSquared = beta * beta;
    
    // ИИ: Защита от деления на ноль при v = c
    if (betaSquared >= 0.9999) 
    {
        betaSquared = 0.9999;
    }
    
    return 1.0 / Math.Sqrt(1.0 - betaSquared);
}

// Использование:
// При v = 0.5c, γ ≈ 1.15 (увеличение массы на 15%)
// При v = 0.9c, γ ≈ 2.29 (увеличение массы в 2.29 раза)
```
```

---

## 5. Специфичные риски для ИИ-агентов

### 5.1. Риски неправильной интерпретации

**🔴 ВЫСОКИЙ РИСК: "Авторитетный сервер" может быть понят неправильно**

```
Неправильная интерпретация ИИ:
"Сервер хранит всё, клиент ничего не считает"
→ ИИ создаст "глупый клиент" без предсказания
→ Лаг будет ужасно заметен

Правильная интерпретация:
"Сервер - источник истины, но клиент предсказывает своё движение и smoothly корректируется"

Рекомендация: Добавить явную диаграмму последовательности для ввода игрока:
1. Игрок нажал W
2. Клиент немедленно применяет ускорение локально (prediction)
3. Клиент отправляет команду на сервер
4. Сервер получает команду через RTT/2
5. Сервер применяет и считает
6. Сервер отправляет snapshot обратно
7. Клиент получает snapshot через RTT/2
8. Клиент сравнивает prediction с server state
9. Если есть расхождение > порога, плавно корректирует
```

**🟡 СРЕДНИЙ РИСК: "Фиксированный timestep" без пояснений**

```
ИИ может реализовать так:
void Update()
{
    float dt = Time.deltaTime; // НЕПРАВИЛЬНО - переменный!
    Physics.Tick(dt);
}

Должно быть:
const float FIXED_DT = 1.0f / 30.0f;
float accumulator = 0;

void Update()
{
    accumulator += Time.deltaTime;
    
    while (accumulator >= FIXED_DT)
    {
        Physics.Tick(FIXED_DT); // ПРАВИЛЬНО - фиксированный шаг
        accumulator -= FIXED_DT;
    }
}

Рекомендация: Добавить пример Fixed Update Loop в M0
```

### 5.2. Риски технического долга

**🔴 ВЫСОКИЙ РИСК: ИИ создаст "God Objects"**

```
Проблема: Без явных границ ИИ может создать:
class GameManager : MonoBehaviour
{
    // 5000 строк кода
    // Всё в одном классе: физика, сеть, UI, ИИ ботов...
}

Рекомендация: Создать architectural guidelines в M0:
- Максимум 300 строк на класс
- Максимум 5 зависимостей на класс
- Обязательные интерфейсы для всех подсистем
- Code review чеклист для ИИ-агентов
```

---

## 6. Конкретные рекомендации по улучшению

### 6.1. Немедленные действия (до начала разработки)

**1. Создать Architecture Decision Records (ADR)**
```markdown
# ADR-001: Выбор Entitas vs собственная ECS

## Статус
Предложено

## Контекст
Нужна ECS для 12 кораблей в M0-M8, возможно расширение до 50+ в M9+

## Рассмотренные варианты
1. Entitas - готовая библиотека
2. Unity DOTS - официальное решение
3. Собственная minimal ECS
4. Чистый OOP

## Решение
ОТЛОЖЕНО до M0 spike (2 недели)

## Критерии выбора
- Работает ли с double precision на сервере?
- Легко ли синхронизировать клиент-сервер?
- Может ли ИИ-агент понять код?

## Последствия
Выбор влияет на все M1-M8
```

**2. Создать "ИИ-Playbook" - руководство для агентов**
```markdown
# ИИ-Playbook для Universe Unlimited

## Как читать документацию
1. Всегда начинай с u2-architecture-v086.md
2. Затем u2-dev-plan-v086.md для этапа
3. Только потом u2-tech-stack для деталей

## Что делать при неопределённости
1. Если не ясно, какую библиотеку использовать → Спроси человека
2. Если не ясен алгоритм → Сделай spike solution и покажи
3. Если возникает архитектурное решение → Создай ADR

## Обязательные проверки перед commit
- [ ] Все тесты проходят
- [ ] Код покрыт тестами минимум на 70%
- [ ] Нет классов больше 300 строк
- [ ] Есть XML-документация для публичных методов
- [ ] Использованы единицы СИ везде

## Запреты
- ❌ НЕ изменять публичные интерфейсы без обсуждения
- ❌ НЕ добавлять библиотеки без обсуждения
- ❌ НЕ оптимизировать до M7 (YAGNI)
- ❌ НЕ использовать магические числа (всё в константы)
```

**3. Настроить автоматические проверки (CI/CD)**
```yaml
# .github/workflows/validation.yml
name: Code Validation

on: [push, pull_request]

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Run unit tests
        run: dotnet test
      - name: Check coverage
        run: |
          dotnet test /p:CollectCoverage=true
          # Fail if coverage < 70%
  
  architecture-compliance:
    runs-on: ubuntu-latest
    steps:
      - name: Check class size
        run: |
          # Провалить если есть файлы > 300 строк
          find . -name "*.cs" -exec wc -l {} \; | awk '$1 > 300 {exit 1}'
      
      - name: Check for magic numbers
        run: |
          # Искать literal numbers в коде
          grep -r "[0-9]\{2,\}" --include="*.cs" | grep -v "const" && exit 1
  
  documentation:
    runs-on: ubuntu-latest
    steps:
      - name: Check XML docs
        run: |
          # Проверить наличие XML documentation
          dotnet build /p:GenerateDocumentationFile=true /warnaserror
```

### 6.2. Изменения в плане разработки

**Предлагаемый исправленный план:**

```
M0 (13-15 недель):
  Week 1-2: ECS Spike
    - Prototypes: Entitas, собственная ECS, чистый OOP
    - Выбор решения на основе метрик
  Week 3-4: Project Setup
    - Структура репозитория
    - CI/CD
    - Coding guidelines для ИИ
  Week 5-8: ECS Implementation
    - Базовые компоненты и системы
    - Юнит-тесты (coverage > 70%)
  Week 9-11: Validation & Номиналы
    - Проверка ТТХ из документа
    - Стресс-тесты
  Week 12-13: Offline Client MVP
    - Минимальный UI
    - 1 корабль, управление WASD
  Week 14-15: Buffer & Documentation
    - Запас на исправление багов
    - Документация для M1

M1 (15-17 недель):
  Week 1-4: Server Physics (double precision)
    - Ньютоновская механика
    - Релятивистская надстройка (γ)
  Week 5-8: Client Physics (float precision)
    - Портирование на Unity
    - Тесты на расхождение с сервером
  Week 9-11: Extended Testing
    - 1000+ ticks без взрывов
    - Проверка инвариантов
    - Performance benchmarks
  Week 12-14: Offline Client v2
    - Улучшенный HUD
    - Сценарии разгона/торможения
  Week 15-17: Stabilization
    - Исправление багов
    - Документация численных параметров
    - Подготовка к M2

M2 (16-18 недель):
  Week 1-2: Protobuf Schema
    - Определение сообщений
    - Генерация кода
  Week 3-6: UDP Transport (только desktop)
    - LiteNetLib integration
    - Handshake, ping, disconnect
  Week 7-10: Client Prediction
    - Предсказание своего корабля
    - Input buffering
  Week 11-14: Reconciliation
    - Smooth correction
    - Replay buffer
  Week 15-16: Testing
    - Симуляция разных RTT (50-500ms)
    - Тесты на packet loss
  Week 17-18: WebSocket (отложено или опционально)
    - Только если необходимо для WebGL

M3-M8: (корректировка сроков после завершения M2)

ИТОГО с буферами: 44-50 недель (11-12 месяцев)
```

### 6.3. Предлагаемые дополнительные документы

**1. Interface Specification Document**
```markdown
# U2 - Interface Specifications v1.0

## IPhysicsEngine
Реализации: ServerPhysicsEngine.cs, ClientPhysicsEngine.cs

### Methods

#### Tick(double deltaTime)
**Назначение:** Обновить физику на один timestep
**Параметры:**
  - deltaTime: ВСЕГДА 1/30 секунды (0.0333...)
**Побочные эффекты:**
  - Обновляет позиции всех сущностей
  - Применяет силы
  - Проверяет коллизии
**Исключения:**
  - ArgumentException если deltaTime != 1/30
**Postconditions:**
  - Все инварианты сохранены (скорость < c', позиции конечны)

(И так для каждого интерфейса)
```

**2. Data Dictionary**
```markdown
# U2 - Data Dictionary

## Ship Component

| Field | Type | Units | Range | Default | Description |
|-------|------|-------|-------|---------|-------------|
| mass | double | кг | 1000-100000 | - | Масса корабля |
| thrust_max | double | Н | 1000-1000000 | - | Максимальная тяга |
| position_x | double | м | unlimited | 0 | X координата |
| position_y | double | м | unlimited | 0 | Y координата |
| velocity_x | double | м/с | -0.9c' to 0.9c' | 0 | X скорость |
| velocity_y | double | м/с | -0.9c' to 0.9c' | 0 | Y скорость |

ИИ: При чтении/записи этих полей проверяй диапазоны!
```

**3. Testing Strategy Document**
```markdown
# U2 - Testing Strategy

## Уровни тестирования

### Unit Tests (70% coverage minimum)
Что тестируем:
- Математические функции (γ-фактор, векторы)
- Отдельные компоненты ECS
- Утилиты

### Integration Tests
Что тестируем:
- Взаимодействие Physics + ECS
- Взаимодействие Network + State Management

### System Tests
Что тестируем:
- Полный game loop (офлайн)
- Client-Server interaction (онлайн)

### Performance Tests
Что тестируем:
- 1000 ticks без деградации FPS
- 12 кораблей при 30Hz без пропусков

## Acceptance Criteria Matrix

| Этап | Must Pass | Should Pass | Nice to Have |
|------|-----------|-------------|--------------|
| M1 | Physics stability tests | Offline client works | - |
| M2 | Network sync tests | RTT < 200ms playable | WebSocket works |
| ... | ... | ... | ... |
```

---

## 7. Итоговые рекомендации

### Приоритет 1: КРИТИЧНО (сделать до начала M0)

1. **Spike на выбор ECS** (2 недели)
   - Entitas vs собственная vs OOP
   - Документировать решение в ADR

2. **Создать Interface Specification Document**
   - Все публичные интерфейсы с контрактами
   - Примеры использования для ИИ

3. **Настроить CI/CD с автоматическими проверками**
   - Тесты, coverage, архитектурные правила

4. **Создать ИИ-Playbook**
   - Как работать с документацией
   - Что делать при неопределённости
   - Список запретов

### Приоритет 2: ВАЖНО (сделать во время M0)

5. **Конкретизировать критерии готовности** для каждого этапа
   - SMART-формат
   - Автоматизируемые тесты

6. **Добавить архитектурные диаграммы**
   - Component diagram
   - Sequence diagrams для ключевых сценариев
   - Timing diagrams для частот обновления

7. **Создать Data Dictionary**
   - Все компоненты ECS с диапазонами
   - Единицы измерения

### Приоритет 3: ЖЕЛАТЕЛЬНО (во время M1-M2)

8. **Написать тестовые кейсы как спецификацию**
   - Acceptance tests для каждого этапа

9. **Добавить примеры кода в документацию**
   - Reconciliation algorithm
   - Fixed timestep loop
   - Input arbitration

10. **Создать визуальный gло для неоднозначных ситуаций**
    - Decision trees
    - Flowcharts

### Корректировка плана

**Текущий план:** 24-30 недель  
**Реалистичный план с буферами:** 44-50 недель (11-12 месяцев)

**Основные изменения:**
- M0: +4 недели (spike + setup + buffer)
- M1: +6 недель (стабилизация физики)
- M2: +8 недель (отложить WebSocket, фокус на стабильности)
- Integration sprints: +3 недели (после M3, M6, M8)

---

## 8. Оценка рисков (сводная таблица)

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Entitas не подходит для архитектуры | 40% | Высокое | Spike в M0, запасной план (собственная ECS) |
| Reconciliation работает плохо | 50% | Критичное | Прототипирование в M2, расширенное тестирование |
| ИИ создаст God Objects | 70% | Среднее | Code review guidelines, автоматические проверки |
| Накопление ошибок float vs double | 30% | Среднее | Тесты на long-running симуляции, настройка reconciliation |
| Недооценка сроков | 80% | Высокое | Буферы 30%, итеративная переоценка после каждого этапа |
| Двойной транспорт (UDP+WS) слишком сложен | 60% | Среднее | Отложить WebSocket до M7-M8 |
| ИИ неправильно поймёт "авторитетный сервер" | 50% | Критичное | Диаграммы последовательности, примеры кода |

---

## Заключение

Проект имеет **прочный фундамент** и **разумные архитектурные решения**. Документация достаточно хороша для человека-разработчика, но **требует существенных дополнений** для эффективной работы ИИ-агентов.

**Ключевое**: Не начинать M1 до завершения всех рекомендаций Приоритета 1. Это сэкономит недели отладки в дальнейшем.

**Прогноз успеха:**
- При текущей документации: 60% (высокий риск переделок)
- С рекомендованными улучшениями: 85% (контролируемые риски)

**Следующие шаги:**
1. Review этого аудита с командой
2. Принять решение по рекомендациям Приоритета 1
3. Обновить план разработки
4. Создать недостающие документы
5. Только после этого начинать M0

---

*Документ подготовлен Claude (Anthropic) 15.11.2025*  
*Версия аудита: 1.0*
