# Universe Unlimited - Action Plan для старта разработки

## 🎯 Цель
Подготовить проект к эффективной разработке с ИИ-агентами за 3-4 недели

---

## Неделя 1: ECS Spike & Infrastructure

### Day 1-2: Создание репозитория и базовой структуры

**Задачи для ИИ-агента:**

```markdown
TASK-001: Создать структуру Unity проекта
Приоритет: P0 (критичный)
Срок: 4 часа

Описание:
Создай Unity проект (LTS 2022.3) со следующей структурой:

/Assets
  /Scripts
    /Core         # ECS, физика, математика
    /Networking   # Сетевой код
    /Gameplay     # Управление, ассистенты
    /UI           # HUD, меню
    /Tests        # Unit и integration тесты
  /Resources      # Временные ассеты (до Addressables)
  /Scenes
    /Development  # Тестовые сцены
    /Game         # Игровые сцены

Критерии приёмки:
- [x] Проект компилируется без ошибок
- [x] Установлен URP (Universal Render Pipeline)
- [x] Установлен Input System package
- [x] Установлен TextMeshPro
- [x] .gitignore настроен для Unity
```

```markdown
TASK-002: Создать .NET сервер проект
Приоритет: P0
Срок: 2 часа

Описание:
Создай .NET 8 console application со следующей структурой:

/Server
  /U2.Server         # Точка входа
  /U2.Core           # Общий код с клиентом
    /Physics
    /Math
    /Entities
  /U2.Networking
  /U2.Tests

Критерии приёмки:
- [x] Проект компилируется на .NET 8
- [x] Установлен NUnit для тестов
- [x] Установлен LiteNetLib для UDP
- [x] Установлен protobuf-net для сериализации
```

### Day 3-5: ECS Spike - Вариант 1 (Entitas)

**Задача для ИИ-агента:**

```markdown
TASK-003: Entitas Spike - Минимальный прототип
Приоритет: P0
Срок: 2 дня

Описание:
Создай прототип на Entitas с одним кораблём:

Компоненты:
- PositionComponent { Vector2d position }
- VelocityComponent { Vector2d velocity }
- ShipComponent { double mass, double thrust }

Системы:
- MovementSystem (применяет скорость к позиции)
- InputSystem (создаёт force из ввода)

Требования:
1. Реализовать на СЕРВЕРЕ (double precision)
2. Реализовать на КЛИЕНТЕ (float precision через Unity)
3. Симулировать 1000 тиков без ошибок
4. Измерить overhead на создание/удаление entities

Критерии приёмки:
- [x] Код компилируется на сервере (.NET 8)
- [x] Код компилируется на клиенте (Unity)
- [x] Тест "1000 тиков без NaN/Infinity" проходит
- [x] Overhead на entity < 1ms на создание
- [x] Можно создать 100 entities одновременно

Результат:
Документ "ECS-Spike-Entitas-Results.md" с:
- Время разработки прототипа
- Сложность интеграции клиент-сервер (1-10)
- Понятность кода для ИИ (1-10)
- Метрики производительности
- Рекомендация: использовать или нет
```

### Day 6-8: ECS Spike - Вариант 2 (Собственная ECS)

**Задача для ИИ-агента:**

```markdown
TASK-004: Custom Minimal ECS - Прототип
Приоритет: P0
Срок: 2 дня

Описание:
Создай минималистичную ECS с нуля:

```csharp
// Базовый дизайн
public class Entity
{
    public int Id;
    public Dictionary<Type, IComponent> Components;
}

public interface IComponent { }

public abstract class EcsSystem
{
    public abstract void Update(double deltaTime);
}

public class World
{
    List<Entity> entities;
    List<EcsSystem> systems;
    
    public void Tick(double dt) 
    {
        foreach (var sys in systems) sys.Update(dt);
    }
}
```

Требования:
1. Те же компоненты, что в TASK-003
2. Полный контроль над кодом
3. Без кодогенерации
4. Работает на сервере и клиенте

Критерии приёмки:
- [x] Реализация < 300 строк кода
- [x] Те же тесты, что для Entitas
- [x] Производительность не хуже Entitas
- [x] Код понятен ИИ (без магии)

Результат:
"ECS-Spike-Custom-Results.md" с аналогичными метриками
```

### Day 9-10: Выбор ECS решения

**Задача для человека (гейм-дизайнера):**

```markdown
DECISION-001: Выбрать ECS решение
Приоритет: P0
Срок: 1 день

Критерии выбора:
1. Понятность для ИИ-агентов (вес: 40%)
2. Производительность (вес: 20%)
3. Простота интеграции клиент-сервер (вес: 30%)
4. Время разработки прототипа (вес: 10%)

Варианты:
[ ] Entitas
[ ] Custom ECS
[ ] Чистый OOP (запасной вариант)

Результат:
ADR-001-ECS-Decision.md с обоснованием
```

---

## Неделя 2: Интерфейсы и CI/CD

### Day 11-12: Определение интерфейсов

**Задача для ИИ-агента:**

```markdown
TASK-005: Создать Interface Specification Document
Приоритет: P1
Срок: 1.5 дня

Описание:
Создай документ с детальным описанием всех интерфейсов для M0-M2

Обязательные интерфейсы:

1. IPhysicsEngine
```csharp
/// <summary>
/// Физический движок для симуляции мира
/// ИИ: Реализуй ОТДЕЛЬНО для сервера (double) и клиента (float)
/// </summary>
public interface IPhysicsEngine
{
    /// <summary>
    /// Обновить физику на фиксированный timestep
    /// </summary>
    /// <param name="deltaTime">
    /// ИИ: ВАЖНО! Всегда передавай ровно 1/30 секунды (0.03333...)
    /// Проверяй: Math.Abs(deltaTime - 1.0/30.0) < 0.0001
    /// </param>
    void Tick(double deltaTime);
    
    /// <summary>
    /// Применить силу к кораблю
    /// </summary>
    /// <param name="entityId">ID сущности</param>
    /// <param name="force">Сила в Ньютонах (СИ!)</param>
    void ApplyForce(int entityId, Vector2d force);
    
    /// <summary>
    /// Получить состояние сущности
    /// </summary>
    /// <returns>
    /// ИИ: Никогда не возвращай null, кидай exception если entity не найдена
    /// </returns>
    IPhysicsState GetState(int entityId);
}
```

2. INetworkTransport
3. IInputSource
4. IShipController

Для каждого интерфейса указать:
- Назначение
- Параметры методов с единицами измерения
- Preconditions (что должно быть true ДО вызова)
- Postconditions (что будет true ПОСЛЕ вызова)
- Исключения
- Пример использования

Критерии приёмки:
- [x] Минимум 10 интерфейсов описано
- [x] Каждый метод имеет пример
- [x] Указаны единицы измерения для всех numeric параметров
- [x] Есть примечания для ИИ-агентов (помечены "ИИ:")
```

### Day 13-14: Настройка CI/CD

**Задача для ИИ-агента:**

```markdown
TASK-006: Настроить GitHub Actions для проверок
Приоритет: P1
Срок: 1.5 дня

Описание:
Создай .github/workflows/ci.yml с проверками:

Проверки для Unity:
1. Компиляция без ошибок
2. Все тесты проходят
3. Code coverage > 70%

Проверки для Server:
1. Компиляция .NET 8
2. Все NUnit тесты проходят
3. Code coverage > 70%

Архитектурные проверки:
4. Нет файлов > 300 строк
5. Нет классов с > 5 зависимостями
6. Все public методы имеют XML-doc
7. Нет magic numbers (literal > 1 цифры без const)

Файл: scripts/check_architecture.py
```python
import os
import re

def check_file_length(filepath, max_lines=300):
    """ИИ: Проверяет, что файл не слишком большой"""
    with open(filepath) as f:
        lines = len(f.readlines())
    if lines > max_lines:
        raise Exception(f"{filepath} has {lines} lines (max {max_lines})")

def check_magic_numbers(filepath):
    """ИИ: Ищет числа в коде, которые должны быть константами"""
    with open(filepath) as f:
        content = f.read()
    # Паттерн: числа вне const/readonly объявлений
    pattern = r'(?<!const )(?<!readonly )\b\d{2,}\b'
    matches = re.findall(pattern, content)
    if matches:
        raise Exception(f"{filepath} contains magic numbers: {matches}")

# ... другие проверки
```

Критерии приёмки:
- [x] CI запускается на каждый push
- [x] Все проверки работают
- [x] Failing builds блокируют merge
- [x] Результаты видны в PR
```

### Day 15: Создание ИИ-Playbook

**Задача для ИИ-агента:**

```markdown
TASK-007: Создать AI-Agent-Playbook.md
Приоритет: P1
Срок: 4 часа

Описание:
Создай руководство для ИИ-агентов со следующими разделами:

1. Как читать документацию проекта
2. Правила кодирования
3. Что делать при неопределённости
4. Список запретов
5. Чеклист перед commit
6. Примеры хорошего и плохого кода

Пример раздела "Что делать при неопределённости":

```markdown
## Когда неясно, КАКУЮ библиотеку использовать

НЕПРАВИЛЬНО:
- Выбрать первую найденную в Google
- Установить 5 библиотек и посмотреть что получится

ПРАВИЛЬНО:
1. Проверить, есть ли рекомендация в u2-tech-stack.md
2. Если нет - создать GitHub Issue с вопросом
3. Подождать ответа человека
4. Задокументировать решение в ADR

Пример Issue:
Title: "Выбор библиотеки для работы с JSON"
Body:
- Задача: Нужно парсить конфигурационные файлы
- Рассмотренные варианты: System.Text.Json, Newtonsoft.Json
- Вопрос: Какой использовать и почему?
```

Критерии приёмки:
- [x] Playbook содержит минимум 20 правил
- [x] Каждое правило имеет пример ПРАВИЛЬНО/НЕПРАВИЛЬНО
- [x] Есть decision trees для частых ситуаций
```

---

## Неделя 3-4: Подготовка M0

### Day 16-20: Базовая имплементация ECS

**Задача для ИИ-агента (после выбора ECS):**

```markdown
TASK-008: Реализовать базовый ECS framework
Приоритет: P0
Срок: 4 дня

Описание:
На основе выбранного решения (Entitas/Custom) реализовать:

Компоненты (минимум):
- PositionComponent
- VelocityComponent  
- ShipComponent
- HealthComponent

Системы (минимум):
- MovementSystem (применяет velocity к position)
- PhysicsSystem (применяет силы, вычисляет acceleration)

Требования:
1. Работает на сервере (double precision)
2. Работает на клиенте (float precision)
3. Общий код вынесен в U2.Core
4. Покрытие тестами > 80%

Тесты (обязательные):
```csharp
[Test]
public void MovementSystem_Should_Update_Position()
{
    // Arrange
    var world = new World();
    var ship = world.CreateEntity();
    ship.Add(new PositionComponent { Value = Vector2d.Zero });
    ship.Add(new VelocityComponent { Value = new Vector2d(10, 0) });
    
    var system = new MovementSystem();
    world.AddSystem(system);
    
    // Act
    world.Tick(1.0 / 30.0); // Один тик
    
    // Assert
    var pos = ship.Get<PositionComponent>();
    Assert.That(pos.Value.X, Is.EqualTo(10.0 / 30.0).Within(0.001));
}

[Test]
public void PhysicsSystem_Should_Not_Explode_Over_1000_Ticks()
{
    var world = CreateWorldWithShip();
    
    for (int i = 0; i < 1000; i++)
    {
        world.Tick(1.0 / 30.0);
    }
    
    var ship = world.GetEntity(0);
    var pos = ship.Get<PositionComponent>().Value;
    var vel = ship.Get<VelocityComponent>().Value;
    
    Assert.That(double.IsNaN(pos.X), Is.False);
    Assert.That(double.IsInfinity(vel.Y), Is.False);
}
```

Критерии приёмки:
- [x] Все компоненты реализованы
- [x] Все системы реализованы
- [x] Работает на сервере
- [x] Работает на клиенте
- [x] Все тесты проходят
- [x] Coverage > 80%
```

### Day 21-25: Математика и физика (заготовки)

**Задача для ИИ-агента:**

```markdown
TASK-009: Реализовать математические утилиты
Приоритет: P0
Срок: 3 дня

Описание:
Создай библиотеку математических функций для физики:

Файл: U2.Core/Math/Vector2d.cs
```csharp
/// <summary>
/// 2D вектор с double precision
/// ИИ: Используется на сервере для точных вычислений
/// </summary>
public struct Vector2d
{
    public double X;
    public double Y;
    
    public double Magnitude => Math.Sqrt(X * X + Y * Y);
    
    public Vector2d Normalized
    {
        get
        {
            double mag = Magnitude;
            // ИИ: Защита от деления на ноль
            if (mag < 1e-10) return new Vector2d(0, 0);
            return new Vector2d(X / mag, Y / mag);
        }
    }
    
    // ... другие методы
}
```

Файл: U2.Core/Physics/RelativisticMath.cs
```csharp
public static class RelativisticMath
{
    /// <summary>
    /// Вычисляет гамма-фактор
    /// </summary>
    /// <param name="velocity">Скорость в м/с</param>
    /// <param name="lightSpeed">Скорость света c' в м/с</param>
    /// <returns>
    /// γ = 1 / sqrt(1 - v²/c'²)
    /// ИИ: При v близкой к c, gamma стремится к бесконечности
    /// </returns>
    public static double CalculateGamma(double velocity, double lightSpeed)
    {
        const double MAX_BETA_SQUARED = 0.9999; // Защита от деления на ноль
        
        double beta = velocity / lightSpeed;
        double betaSquared = beta * beta;
        
        // ИИ: Ограничиваем максимальную скорость
        if (betaSquared >= MAX_BETA_SQUARED)
        {
            betaSquared = MAX_BETA_SQUARED;
        }
        
        return 1.0 / Math.Sqrt(1.0 - betaSquared);
    }
    
    /// <summary>
    /// Вычисляет релятивистскую силу
    /// </summary>
    /// <remarks>
    /// ИИ: При больших скоростях сила "уменьшается" из-за роста массы
    /// F_rel = F_classical / γ³
    /// </remarks>
    public static double ApplyRelativisticCorrection(
        double classicalForce, 
        double velocity, 
        double lightSpeed)
    {
        double gamma = CalculateGamma(velocity, lightSpeed);
        double gammaCubed = gamma * gamma * gamma;
        return classicalForce / gammaCubed;
    }
}
```

Тесты (обязательные):
- Gamma = 1 при v = 0
- Gamma > 1 при любой v > 0
- Gamma стремится к бесконечности при v → c
- Защита от v > c работает

Критерии приёмки:
- [x] Vector2d полностью реализован
- [x] RelativisticMath реализован
- [x] Все тесты проходят
- [x] Coverage > 90%
```

### Day 26-30: Прототип офлайн-клиента

**Задача для ИИ-агента:**

```markdown
TASK-010: Создать минимальный офлайн клиент
Приоритет: P1
Срок: 4 дня

Описание:
Создай Unity сцену с одним кораблём:

Сцена: Assets/Scenes/Development/Offline_Test.unity
- Камера (ортографическая, вид сверху)
- Корабль (простой спрайт или 3D модель)
- Простой фон (звёзды)

Скрипты:
1. OfflineGameManager.cs
   - Создаёт World
   - Добавляет тестовый корабль
   - Вызывает World.Tick() в FixedUpdate

2. ShipRenderer.cs
   - Читает PositionComponent
   - Обновляет transform.position

3. SimpleInput.cs
   - WASD для thrust
   - Создаёт InputCommand

4. DebugHUD.cs (простейший)
   - Скорость
   - Позиция
   - FPS

Требования:
- Корабль двигается по WASD
- Физика работает (30 Hz)
- Рендер плавный (60 FPS)
- Нет ошибок в консоли

Критерии приёмки:
- [x] Сцена работает без ошибок
- [x] Корабль управляется
- [x] HUD показывает данные
- [x] Стабильный FPS > 55
```

---

## Чеклист готовности к M1

После выполнения всех задач, проверь:

### Документация
- [ ] Interface Specification готов
- [ ] AI-Agent-Playbook готов
- [ ] ADR-001 (выбор ECS) написан
- [ ] Все интерфейсы имеют примеры

### Инфраструктура
- [ ] Репозиторий создан и настроен
- [ ] CI/CD работает
- [ ] Все проверки включены
- [ ] Code coverage tracking настроен

### Код
- [ ] ECS framework реализован
- [ ] Базовая математика работает
- [ ] Офлайн клиент запускается
- [ ] Все тесты проходят (coverage > 70%)

### Процесс
- [ ] Правила кодирования зафиксированы
- [ ] Code review process определён
- [ ] Issue tracking настроен
- [ ] Git workflow определён (feature branches, PR, etc.)

---

## Метрики успеха

После этих 4 недель у вас должно быть:

| Метрика | Целевое значение |
|---------|-----------------|
| Тестов написано | > 50 |
| Code coverage | > 70% |
| Документов создано | > 10 |
| Интерфейсов определено | > 15 |
| Прототипов собрано | 2-3 (ECS варианты) |
| CI/CD проверок | > 7 |
| Рабочих Unity сцен | > 1 |
| Архитектурных решений | > 3 (ADR) |

---

## Следующий шаг после завершения

Когда все задачи выполнены:
1. Провести ретроспективу
2. Оценить реальную скорость разработки
3. Скорректировать оценки для M1-M8
4. Начать M1 с уверенностью в фундаменте

---

*Этот план создан на основе аудита v1.0*  
*Актуален на: 15.11.2025*
