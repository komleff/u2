# U2 — Архитектура v0.8.6 Minimal

**Версия:** 0.8.6 Minimal  
**Дата:** 2025-11-18  
**Подход:** Минимизация рисков, фундамент с возможностью расширения

Документ описывает архитектуру прототипа космического симулятора **Universe Unlimited** с минимальным подходом: как устроен игровой мир с замедленной скоростью света, как взаимодействуют клиент и сервер, как рассчитывается физика, управление и помощник пилота (только режим Stabilized в M0-M6).

**⚠️ ВАЖНО: Минимальный подход**

Этот документ описывает **минимальную версию архитектуры (M0-M6)**, необходимую для проверки ключевых концепций:
- Релятивистская физика с γ-фактором
- Flight Assist ON vs OFF
- Сеть + reconciliation
- Примитивные системы (урон, боты)

Сложные системы **НЕ реализованы** в M0-M6 и будут добавлены позже (M7+) через расширяемые интерфейсы.

Текст рассчитан на архитекторов и разработчиков клиент-серверных приложений.

Архитектура согласована с:
- Планом разработки **"U2 — План для ИИ-агентов v0.8.6 Minimal"**
- Документом **"Ship Tech Specs v0.8.6 Minimal"** (обновлённая спецификация ТТХ)
- Упрощённой моделью мира: 2D проекция, авторитетный сервер, фиксированный c′

---

## Система единиц

Все расчёты ведутся в **СИ** (Международная система единиц):

| Величина | Единица | Обозначение |
|----------|---------|-------------|
| Расстояние | метр | м |
| Время | секунда | с |
| Масса | килограмм (тонны) | кг (т) |
| Скорость | метр в секунду | м/с |
| Ускорение | метр на секунду² | м/с² |
| Сила/тяга | ньютон (меганьютон) | Н (МН) |

**Перегрузка (g):**
- Используется для удобства восприятия
- 1g = 9.81 м/с²
- Перегрузка = ускорение / 9.81

**Скорость света:**
- Измеряется в м/с
- Типичные значения: 1,000 – 10,000 м/с (зоны боя)
- Высокие значения: до 100,000 м/с (межзвёздные области)
- Реальная скорость света: ~300,000,000 м/с (не используется в игре)

---

## **0. Назначение и границы документа**

### Цели документа (Minimal версия)

1. Зафиксировать целевую архитектуру **минимальной версии игры (M0-M6)** для проверки фундаментальных концепций
2. Описать ключевые технические решения: мир с замедленным светом, физика, FA:ON/OFF
3. Дать общую картину для всех участников команды

### Что реализуется в M0-M6 (фундамент)

✅ **Релятивистская физика** с γ-фактором  
✅ **FA:ON vs FA:OFF** (ключевая концепция v0.8.6)  
✅ **Сеть + reconciliation** (client-side prediction)  
✅ **Примитивный урон** (hull_HP, фиксированный)  
✅ **Примитивные боты** (случайное движение)  
✅ **Минимальный HUD** (без голограмм)  
✅ **Stabilized ассистент** (один режим)

### Что НЕ реализуется в M0-M6 (отложено до M7+)

❌ **Детальная тепловая модель** → M7 (заглушка: temperature = const)  
❌ **Щиты и броня** → M8 (новая концепция, интерфейсы готовы)  
❌ **P_hit и ТТХ оружия** → M9 (примитивный урон: 100 HP фиксированно)  
❌ **Система сканирования** → M9 (нет signature_km в M0-M6)  
❌ **Умные боты** → M10 (примитивные боты: случайное управление)  
❌ **Coupled-ассистенты** → M11 (только Stabilized в M0-M6)  
❌ **Наблюдение с задержкой света** → M11 (прямое отображение в M0-M6)  
❌ **Асинхронные системы** → M7+ (thrust allocation, детальное тепло)

### Документ НЕ описывает

* Детали реализации пользовательского интерфейса
* Полный перечень видов оружия, кораблей и экономических механик
* Конкретный выбор библиотек и фреймворков

Эти детали оформляются в отдельных документах.

---

## **1. Итоговое видение минимальной версии**

Минимальная версия нужна, чтобы как можно раньше проверить «скелет» игры: физику полёта, управление FA:ON/OFF, сеть и помощника пилота.

### **1.1. Масштаб мира и нагрузка**

* Одна звёздная система, представляемая в двумерной плоскости (вид сверху)
* Один сервер мира обслуживает небольшой «остров» вселенной (шард)
* Целевая нагрузка для минимальной версии:
  * **4 игрока** (хозяин + до 3 приглашённых друзей)
  * **8 ботов** (4 в зоне активного боя + 4 в резерве)
  * **Итого: 12 кораблей**, из них 8 в близком контакте одновременно

**Модель сессий:**
* **PvE кооператив:** персональная вселенная игрока с приглашением до 3 друзей
* **PvP арена:** 4v4 (отдельный режим, вне минимальной версии)
* **Корпоративные войны:** 25v25 (будущее расширение, требует шардирования)

**Обоснование модели:**  
90% игроков в космосимах занимаются PvE и избегают PvP. Модель "персональная вселенная + кооп" (как в Genshin Impact, SnowRunner) позволяет сфокусироваться на качестве геймплея без требований к масштабированию MMO-уровня.

Этого достаточно, чтобы проверить:
* стабильность физики и управления FA:ON/OFF
* корректность сетевого обмена и скрытие задержек
* работу примитивной боевой системы и помощника пилота

### **1.2. Игровой цикл и офлайн-режим**

Для разработки и отладки важен **офлайн-режим**:
* симуляция мира и управление кораблём работают на одной машине
* разработчик может воспроизводить сложные полётные ситуации без поднятия сервера
* поведение корабля в офлайн-режиме должно быть близким к онлайн-режиму

**Офлайн-режим доступен ТОЛЬКО разработчикам, не игрокам.**

**Важно:** Офлайн-режим не обязан побитово совпадать с онлайн-режимом. Допустимы упрощения (например, отключение reconciliation), если они не влияют на отладку физики и управления. Цель офлайн-режима — быстрая итерация при разработке.

### **1.3. Клиент, сервер и авторитетность**

Онлайн-режим использует ту же физику и логику, но вводит:
* Мир рассчитывается на сервере. Сервер хранит «истинное» состояние
* Клиент отображает мир, принимает ввод игрока, делает краткосрочные прогнозы (client-side prediction)
* Любые важные решения по физике, урону и проверке правил принимаются на сервере

---

## **2. Частоты работы системы, задержки и буферы**

Разные части системы работают с разной частотой.

### **2.1. Основные частоты (Minimal версия)**

**Шаг физики на сервере:** 30 Hz (фиксировано)

**Синхронная симуляция (каждый тик, 30 Hz):**
* Физика движения (релятивистская)
* Управление и Flight Assist
* Примитивные коллизии

**Обновление состояния по сети:**
* **Базовая частота:** 15 Hz (сетевые снимки от сервера к клиентам)
* Клиент всегда интерполирует между пришедшими снапшотами

**Команды управления от клиента:** 30 Hz

**Кадры изображения на клиенте:** 60 FPS

**Клиентский предсказатель:** 60 Hz для сглаживания движения

⚠️ **В M0-M6 НЕТ асинхронных систем:**
- ❌ Нет детальной тепловой модели (заглушка: T = const)
- ❌ Нет thrust allocation (прямое применение тяги)
- ❌ Нет сложного AI (примитивные боты на 30 Hz)

**Принцип:** Сервер считает физику часто (30 Hz), сеть отправляет состояния реже (15 Hz), клиент дорисовывает движение между состояниями (60 FPS).

### **2.2. Задержки и очереди состояний**

Задержка между сервером и клиентом состоит из:
* времени передачи данных по сети
* времени обработки пакета на сервере и клиенте
* времени до ближайшего шага браузера или игрового цикла

Чтобы движение кораблей казалось плавным, клиент использует **очередь будущих состояний**:
* состояния чужих кораблей отображаются с небольшой задержкой (50-150 мс)
* клиент успевает заранее получить несколько состояний и плавно интерполировать движение

Свой корабль клиент **предсказывает** (client-side prediction). При расхождении с серверным состоянием применяется reconciliation (мягкая подтяжка).

⚠️ **В M0-M6:** Наблюдение с задержкой света НЕ реализовано (будет в M11+). Корабли отображаются напрямую с буфером интерполяции.

---

## **3. Мир с замедленным светом**

В игре используется модель, где **скорость света значительно ниже привычной**.

### **3.1. Базовая скорость света**

В игре мы искусственно **снижаем скорость света** до значений, при которых задержка становится чувствительной:
* Для удобства расчётов базовая скорость света принимается порядка **5,000-10,000 м/с**
* Это значение достаточно велико для манёвров, но достаточно мало для заметных эффектов относительности

### **3.2. Фиксированная скорость света в локации**

В минимальной версии скорость света **фиксирована для каждой локации**:

* **Тестовые локации:** c′ = 1,000 м/с (для отладки)
* **Зоны боя / околопланетное пространство:** c′ = 5,000 м/с
* **Открытый космос:** c′ = 10,000-100,000 м/с (для будущих версий)

**Почему фиксированный c′:**
* Упрощает физику (не нужно пересчитывать γ при изменении c′)
* Упрощает балансировку (каждая локация настроена на определённый темп боя)
* Позволяет создавать разные игровые ощущения в разных локациях

**В будущем (M11+):** Возможно введение переменного c′ для специальных зон (гравитационные аномалии, варп-пузыри).

### **3.3. Предельная скорость для корабля**

Скорость корабля не может превысить скорость света в данной локации:
* Физический движок автоматически ограничивает скорость до 0.999c′
* При попытке разогнаться дальше γ стремится к бесконечности
* Дополнительная тяга почти не увеличивает скорость, только расходует энергию

---

## **4. Физика движения (релятивистская кинематика)**

Движение кораблей рассчитывается по упрощённой релятивистской модели.

### **4.1. Основная формула интегратора**

На каждом шаге симуляции (dt = 1/30 с):

```
1. Расчёт силы:
   F = CalculateForce(entity)  // из управления + FA

2. Интеграция импульса:
   p_new = p_old + F * dt

3. Преобразование p → v с учётом γ:
   v_classic = |p| / m
   beta = v_classic / c′
   if beta >= 0.999: beta = 0.999  // предохранитель
   
   gamma = 1 / sqrt(1 - beta²)
   v = p / (gamma * m)

4. Интеграция позиции:
   x_new = x_old + v * dt

5. Угловая динамика (без γ):
   L_new = L_old + torque * dt
   omega = L / I
   angle_new = angle_old + omega * dt
```

### **4.2. Расчёт силы (FA:ON vs FA:OFF)**

**FA:OFF (свободный полёт):**
```csharp
Vector2 CalculateForce_FA_OFF(entity) {
  var config = entity.shipConfig.config;
  var control = entity.controlState;
  float mass = entity.mass.mass_kg;
  
  // Прямое применение из physics.*
  float thrust_accel = control.thrust > 0 
    ? control.thrust * config.Physics.LinearAcceleration.Forward
    : -control.thrust * config.Physics.LinearAcceleration.Reverse;
  
  float strafe_accel = control.strafe_x * config.Physics.StrafeAcceleration.Lateral;
  
  // Преобразование в мировые координаты
  Vector2 forward = new Vector2(cos(angle), sin(angle));
  Vector2 right = new Vector2(-forward.y, forward.x);
  
  Vector2 accel = thrust_accel * forward + strafe_accel * right;
  return accel * mass;
}
```

**FA:ON (стабилизированный полёт):**
```csharp
Vector2 CalculateForce_FA_ON(entity) {
  // 1. Базовая сила из управления (как в FA:OFF)
  Vector2 base_force = CalculateForce_FA_OFF(entity);
  
  // 2. Добавить стабилизирующую силу
  Vector2 stabilizing_force = FlightAssistSystem.Calculate(entity);
  
  return base_force + stabilizing_force;
}
```

⚠️ **В M0-M6:** Нет детального тепла, нет thrust allocation (виртуального распределения). Сила применяется напрямую.

### **4.3. Сохранение энергии и инварианты**

**Инварианты:**
* Скорость не превышает 0.999c′
* Масса остаётся постоянной (нет расхода топлива в M0-M6)
* Энергия сохраняется с точностью ±1%

⚠️ **В M0-M6:** Нет расхода топлива, нет детальной энергетической модели (заглушки для M7+).

---

## **5. Управление: Flight Assist ON vs OFF**

**КЛЮЧЕВАЯ КОНЦЕПЦИЯ v0.8.6**

### **5.1. Режим FA:OFF (свободный полёт)**

**Поведение:**
* Команды игрока напрямую масштабируются в тягу/момент
* Максимальные ускорения из `physics.linearAcceleration_mps2`
* Линейные и угловые скорости **НЕ ограничиваются** (кроме 0.999c′)
* Корабль летит по инерции
* При отпускании управления корабль **НЕ гасит** скорость

**Применение:**
* Гонки
* Сложные манёвры (дрифт, резкие развороты)
* Опытные пилоты

### **5.2. Режим FA:ON (стабилизированный полёт)**

**Поведение:**
* Автопилот стабилизирует корабль
* Те же ускорения из `physics.*`
* Скорости **ограничиваются** `flightAssistLimits.linearSpeedMax_mps`
* Автопилот старается не превышать `crewGLimit.linear_g`
* При отпускании управления корабль **гасит** скорость и останавливает вращение

**Применение:**
* Новички
* Комфортный полёт
* Точное наведение

### **5.3. Переключение FA:ON/OFF**

**Интерфейс:**
```csharp
// Игрок нажимает Z
if (Input.GetKeyDown(KeyCode.Z)) {
  player.flightAssist.enabled = !player.flightAssist.enabled;
}

// HUD индикация
hudText.text = player.flightAssist.enabled ? "FA: ON" : "FA: OFF";
hudText.color = player.flightAssist.enabled ? Color.green : Color.yellow;
```

**Сетевой протокол:**
```protobuf
message ManeuverCommand {
  uint32 entity_id = 1;
  uint32 tick = 2;
  float thrust = 3;
  float strafe_x = 4;
  float yaw_input = 5;
  bool flight_assist = 6;  // FA:ON или FA:OFF
}
```

---

## **6. Ассистент пилота (Minimal версия)**

В M0-M6 реализован **только один режим: Stabilized**.

### **6.1. Stabilized (единственный режим в M0-M6)**

**Цель:** Стабилизировать корабль, гасить скорость при отпускании управления.

**Алгоритм (упрощённый PD-контроллер):**
```csharp
public class FlightAssistSystem : IExecuteSystem {
  public void Execute() {
    foreach (var entity in entities) {
      if (entity.flightAssist.enabled) {
        StabilizeFA_ON(entity);
      }
      // FA:OFF: ничего не делаем
    }
  }
}

void StabilizeFA_ON(GameEntity entity) {
  var config = entity.shipConfig.config;
  var limits = config.FlightAssistLimits;
  
  // 1. Ограничение линейных скоростей
  Vector2 vel_local = WorldToLocal(entity.velocity.linear, entity.transform2D.rotation);
  
  if (vel_local.x > limits.LinearSpeedMax.Forward) {
    // Применить тормозящую силу
    ApplyBrakingForce(entity, vel_local);
  }
  
  // 2. Стабилизация вращения (PD-контроллер)
  float target_omega = entity.controlState.yaw_input * limits.AngularSpeedMax.Yaw;
  float error = target_omega - entity.velocity.angular;
  float torque = Kp * error;  // упрощённо, без Kd пока
  
  ApplyTorque(entity, torque);
}
```

**Упрощения в M0-M6:**
* Без jerk-лимитирования
* Без детального контроля g-перегрузок (только порог `crewGLimit.linear_g`)
* Без эскалации влияния (будет в M5-M6 опционально)

### **6.2. Что НЕ реализовано в M0-M6 (Coupled-ассистенты)**

❌ **Interceptor** (агрессивная атака) → M11  
❌ **Evade** (уклонение) → M11  
❌ **Escort** (сопровождение) → M11  
❌ **Dock** (стыковка) → M12

Эти режимы будут добавлены через интерфейсы:
```csharp
// Будущее (M11+)
public interface IAssistMode {
  void Execute(GameEntity entity);
}

public class InterceptorAssist : IAssistMode {
  // Атакующий режим с предиктивным прицеливанием
}
```

---

## **7. Тепловая модель (Minimal версия)**

### **7.1. Заглушка для M0-M6**

В минимальной версии **тепловая модель НЕ реализована**:

```csharp
public class SimpleThermalSystem {
  public void Execute() {
    // entity.heat.temperature = 300;  // всегда норма
    // НЕТ нагрева от двигателей
    // НЕТ охлаждения
    // НЕТ перегрева и троттлинга
  }
}
```

**Почему заглушка:**
* Детальная тепловая модель сложна (нагрев, охлаждение, троттлинг)
* В M0-M6 фокус на физике и FA:ON/OFF
* Тепло добавит мало геймплея на этом этапе

### **7.2. Расширение в M7+ (детальная модель)**

Будущий интерфейс:
```csharp
public interface IThermalSystem {
  void UpdateHeat(GameEntity entity, float dt);
  bool IsOverheating(GameEntity entity);
  float GetThrottlingFactor(GameEntity entity);
}

public class DetailedThermalSystem : IThermalSystem {
  public void UpdateHeat(GameEntity entity, float dt) {
    // Нагрев от двигателей
    // Нагрев от оружия
    // Охлаждение
    // Повреждения при перегреве
  }
}
```

---

## **8. Сетевой обмен**

### **8.1. Протокол (Protobuf)**

**Минимальные сообщения:**
```protobuf
message ManeuverCommand {
  uint32 entity_id = 1;
  uint32 tick = 2;
  float thrust = 3;
  float strafe_x = 4;
  float yaw_input = 5;
  bool flight_assist = 6;  // FA:ON/OFF
}

message EntitySnapshot {
  uint32 entity_id = 1;
  Vec2 position = 2;
  float rotation = 3;
  Vec2 velocity = 4;
  bool flight_assist = 5;
}

message WorldSnapshot {
  uint32 tick = 1;
  repeated EntitySnapshot entities = 2;
}
```

**Упрощения в M0-M6:**
* Без адаптивного битрейта
* Без компрессии
* Фиксированные частоты: 30 Hz команды, 15 Hz снимки

### **8.2. Client-side prediction и reconciliation**

**Prediction:**
```csharp
public class ClientPrediction {
  public void Predict() {
    // Локальная копия физики
    // Применить свои команды вперёд
    // Отобразить результат без ожидания сервера
  }
}
```

**Reconciliation:**
```csharp
public class ClientReconciliation {
  public void Reconcile(ServerSnapshot snapshot) {
    // Сравнить предсказание с серверным состоянием
    // Если расхождение > threshold:
    //   - Откатить до серверного состояния
    //   - Переиграть свои команды с момента snapshot.tick
    // Мягкая подтяжка за 3-5 кадров
  }
}
```

---

## **9. Бой (Minimal версия)**

### **9.1. Примитивный урон (M5)**

В M0-M6 реализована **примитивная система урона**:

```csharp
public interface IDamageSystem {
  void ApplyDamage(uint target_id, float damage);
}

// Minimal: фиксированный урон
public class SimpleDamageSystem : IDamageSystem {
  public void ApplyDamage(uint target_id, float damage) {
    var target = GetEntity(target_id);
    target.health.current_HP -= 100.0f;  // фиксированно
    
    if (target.health.current_HP <= 0) {
      target.Add<DestroyedFlag>();
    }
  }
}
```

**Стрельба:**
```protobuf
message FireCommand {
  uint32 shooter_id = 1;
  uint32 target_id = 2;
}

// На сервере
if (fireCommand.Received) {
  damageSystem.ApplyDamage(cmd.target_id, 100.0f);
}
```

**Что НЕ реализовано:**
* ❌ P_hit (вероятность попадания) → M9
* ❌ Щиты и броня → M8
* ❌ ТТХ оружия (калибры, урон по типу) → M9
* ❌ Детальная баллистика → M9

### **9.2. Расширение в M8+ (щиты и броня)**

Будущие интерфейсы:
```csharp
public interface IShieldSystem {
  void AbsorbDamage(uint entity_id, float damage);
  bool IsPenetrated(uint entity_id, float damage);
}

public interface IArmorSystem {
  float ReduceDamage(uint entity_id, float damage);
}

// Будущая система урона (M9+)
public class AdvancedDamageSystem : IDamageSystem {
  public void ApplyDamage(uint target_id, float damage) {
    // 1. Проверить P_hit
    // 2. Пробить щиты
    // 3. Снизить урон бронёй
    // 4. Применить к корпусу
    // 5. Проверить критические попадания
  }
}
```

---

## **10. AI и боты (Minimal версия)**

### **10.1. Примитивные боты (M5)**

В M0-M6 реализованы **примитивные боты** со случайным управлением:

```csharp
public interface IBotAI {
  void MakeDecision(GameEntity bot);
}

// Minimal: случайное управление
public class RandomBotAI : IBotAI {
  public void MakeDecision(GameEntity bot) {
    bot.controlState.thrust = Random.Range(-1f, 1f);
    bot.controlState.strafe_x = Random.Range(-1f, 1f);
    bot.controlState.yaw_input = Random.Range(-1f, 1f);
    
    // Случайная стрельба
    if (Random.value < 0.01f && bot.weapon.ready) {
      var targets = GetNearbyEnemies(bot);
      if (targets.Count > 0) {
        SendFireCommand(bot.id, targets[0].id);
      }
    }
  }
}
```

**Альтернатива (чуть умнее):**
```csharp
// Патруль по случайным точкам
public class PatrolBotAI : IBotAI {
  public void MakeDecision(GameEntity bot) {
    Vector2 waypoint = bot.ai.currentWaypoint;
    if (Distance(bot, waypoint) < 100) {
      bot.ai.currentWaypoint = RandomPoint();
    }
    FlyTowards(bot, waypoint);
  }
}
```

**Что НЕ реализовано:**
* ❌ Тактика (атака, отступление, позиционирование) → M10
* ❌ Кооперация (командная работа) → M10
* ❌ Уклонение (dodge, jink) → M10
* ❌ Предиктивное прицеливание → M10

### **10.2. Расширение в M10+ (умные боты)**

Будущие интерфейсы:
```csharp
public interface ITacticalAI : IBotAI {
  void Attack(GameEntity bot, GameEntity target);
  void Retreat(GameEntity bot);
  void Patrol(GameEntity bot, Vector2[] waypoints);
  void Dodge(GameEntity bot);
}

public class TacticalBotAI : ITacticalAI {
  // Атака с тактикой
  // Отступление при низком HP
  // Кооперация с союзниками
  // Предиктивное прицеливание
}
```

---

## **11. HUD и наблюдение (Minimal версия)**

### **11.1. Минимальный HUD (M4)**

В M0-M6 реализован **базовый HUD** без голограмм:

```csharp
// Скорость
float speed = player.velocity.linear.Magnitude;
speedText.text = $"{speed:F0} м/с";

// Ускорение
float g = accel.Magnitude / 9.81f;
accelText.text = $"{g:F1} g";

// Курс
float heading = player.transform2D.rotation * Mathf.Rad2Deg;
headingText.text = $"{heading:F0}°";

// FA статус
faText.text = player.flightAssist.enabled ? "FA: ON" : "FA: OFF";
faText.color = player.flightAssist.enabled ? Color.green : Color.yellow;

// Отладка (F3)
if (Input.GetKey(KeyCode.F3)) {
  debugText.text = $"FPS: {1.0f / Time.deltaTime:F0}\n" +
                   $"RTT: {connection.RTT:F0} ms";
}
```

**Что НЕ реализовано:**
* ❌ Голограммы будущего положения → M11
* ❌ Наблюдение с задержкой света → M11
* ❌ Сложные визуализации траекторий → M11

### **11.2. Наблюдение (Minimal версия)**

В M0-M6 корабли отображаются **напрямую** (без задержки света):
* Используется буфер интерполяции (50-150 мс) для сглаживания
* Нет расчёта задержки по формуле `distance/c′`
* Нет голограмм будущего положения

**Расширение в M11+:**
```csharp
// Будущая система (M11+)
public class LightDelayObserver {
  public void UpdateObservation(GameEntity observer, GameEntity target) {
    float distance = Distance(observer, target);
    float light_delay = distance / c_prime;
    
    // Показать target в прошлом на light_delay
    // + голограмма будущего положения
  }
}
```

---

## **12. Система сканирования (НЕ реализована в M0-M6)**

### **12.1. Заглушка для M0-M6**

В минимальной версии **нет системы сканирования**:
* ❌ Нет поля `signature_km` в Ship Tech Specs v0.8.6 Minimal
* ❌ Нет расчёта дальности обнаружения
* ❌ Нет стелса

**Все корабли видны всегда** в пределах зоны отрисовки.

### **12.2. Расширение в M9+ (детальное обнаружение)**

Будущий интерфейс:
```csharp
public interface IScanningSystem {
  bool CanDetect(uint observer_id, uint target_id);
  float GetDetectionRange(uint observer_id, uint target_id);
}

public class DetailedScanningSystem : IScanningSystem {
  public bool CanDetect(uint observer_id, uint target_id) {
    var target_ship = GetShip(target_id);
    float signature = target_ship.config.Hull.signature_km * 1000; // м
    
    // Для стелса: signature *= 0.5
    float distance = Distance(observer_id, target_id);
    
    return distance < signature;
  }
}
```

**Ship Tech Specs M9+ (будущее):**
```json
{
  "hull": {
    "dryMass_t": 10,
    "hull_HP": 1000,
    "signature_km": 9.5  // добавится в M9+
  },
  "classification": {
    "stealth": "standard"  // или "stealth" (x0.5)
  }
}
```

---

## **13. Тестирование и наблюдаемость**

### **13.1. Тесты для M0-M6**

**Обязательные тесты:**
* Unit-тесты: γ-фактор, интегратор, FA:ON/OFF
* Integration-тесты: сеть + reconciliation, примитивный урон
* Property-based тесты (FsCheck): сохранение энергии, инварианты скорости

**Офлайн-режим:**
* Используется для отладки физики и управления
* Доступен только разработчикам

### **13.2. Наблюдаемость**

Сервер и клиенты передают телеметрию:
* Частоты шагов симуляции (должно быть ~30 Hz)
* Задержки сети (RTT, jitter)
* Расход процессорного времени и памяти
* Количество активных объектов (12 кораблей в M0-M6)

---

## **14. Значения «по умолчанию» для минимальной версии**

**Скорость света (фиксирована для локации):**
* Тестовая локация: c′ = 1,000 м/с
* Околопланетный бой: c′ = 5,000 м/с
* Астероидные поля: c′ = 10,000 м/с

**Частоты симуляции:**
* Шаг физики сервера: **30 Hz** (33.3 мс на тик)
* Обновления по сети: 15 Hz (66.7 мс между снапшотами)
* Команды управления от клиента: 30 Hz
* Отрисовка клиента: 60 FPS

**Сетевые буферы:**
* Буфер будущих состояний на клиенте: 100-150 мс
* Задержка отображения чужих кораблей: ~RTT/2 + buffer

**Типичные корабли для тестов (из Ship Tech Specs v0.8.6 Minimal):**
* light interceptor (10 т, 90 м/с²): быстрый и манёвренный
* light fighter (25 т, 70 м/с²): базовый истребитель
* medium courier (70 т, 50 м/с²): быстрый гражданский
* heavy freighter (500 т, 20 м/с²): медленный грузовой

**Рекомендуемые значения c′ для тестов:**
* c′ = 1,000 м/с: для отладки (макс. скорость ~900 м/с)
* c′ = 5,000 м/с: для боя (макс. скорость ~4,500 м/с)
* c′ = 10,000 м/с: для гонок (макс. скорость ~9,000 м/с)

**При c′ = 5,000 м/с и accel = 80 м/с²:**
* Время до 0.5c′ (2,500 м/с): ~31 секунда
* Время до 0.8c′ (4,000 м/с): ~50 секунд
* γ при 0.8c′: ~1.67

---

## **15. Roadmap расширений (M7+)**

После построения фундамента (M0-M6):

**M7: Детальная тепловая модель**
* Нагрев от двигателей и оружия
* Охлаждение, перегрев, троттлинг
* Интерфейс: `IThermalSystem`

**M8: Щиты и броня**
* Ждём финализации концепции
* Интерфейсы: `IShieldSystem`, `IArmorSystem`

**M9: Продвинутый бой**
* P_hit (вероятность попадания)
* ТТХ оружия, калибры
* Система сканирования (signature_km, стелс)
* Интерфейс: `IScanningSystem`

**M10: Умные боты**
* Тактика (атака, отступление, патруль)
* Кооперация, уклонение
* Интерфейс: `ITacticalAI`

**M11: Наблюдение с задержкой света**
* Очередь будущих состояний
* Экстраполяция движения
* Голограммы будущего положения

**M12: Coupled-ассистенты**
* Interceptor, Evade, Escort, Dock
* Интерфейс: `IAssistMode`

**Каждое расширение — отдельный модуль с чёткими интерфейсами**

---

## **Глоссарий**

**Игровой мир**  
Математическая модель космоса, в которой рассчитываются положение и движения кораблей, действие сил и скорость света.

**Шаг симуляции**  
Один цикл вычислений физики и логики на сервере. На каждом шаге пересчитываются положения и состояния объектов.

**Скорость света c′**  
Эффективная скорость распространения сигналов в игровом мире. Намеренно занижена и фиксирована для каждой локации.

**Фактор γ (гамма)**  
Число, показывающее, насколько сильно проявляются эффекты относительности: γ = 1 / sqrt(1 - beta²).

**Перегрузка**  
Отношение ускорения к g = 9.81 м/с². Показывает, насколько «давит» манёвр на экипаж.

**Flight Assist ON/OFF**  
Режимы управления: FA:OFF — свободный полёт по инерции, FA:ON — стабилизация и ограничение скоростей.

**Client-side prediction**  
Расчёт клиентом своего состояния вперёд без ожидания сервера.

**Reconciliation**  
Процесс корректировки клиентского предсказания при получении серверного состояния.

**Snapshot (снимок состояния)**  
Полное или частичное состояние игрового мира в определённый момент, отправляемое сервером клиентам.

**RTT (Round-Trip Time)**  
Время прохождения сигнала от клиента до сервера и обратно. Типичные значения: 50-150 мс.

**Hull HP (очки корпуса)**  
Основные очки прочности корабля. В M0-M6 используется для примитивного урона (фиксированный: 100 HP за выстрел).

**Примитивный урон**  
Упрощённая система урона без P_hit, щитов и брони. Используется в M0-M6.

**Заглушка**  
Упрощённая реализация системы с интерфейсом для будущего расширения.

---

## **ПРИЛОЖЕНИЕ: Связанные документы**

Для полной картины архитектуры и разработки см. также:

1. **"Ship Tech Specs v0.8.6 Minimal"** — Обновлённая спецификация ТТХ кораблей (без signature_km, щитов, брони)
2. **"U2 — План разработки Minimal v0.8.6"** — Пошаговый план реализации M0-M6 (19-27 недель)
3. **"U2 — Executive Summary v0.8.6"** — Обоснование минимального подхода и сравнение планов

---

## **Changelog v0.8.6 Minimal**

**v0.8.6 Minimal (2025-11-18):**
- ✅ Убраны избыточные элементы из архитектуры:
  - ❌ Детальная тепловая модель → M7 (заглушка)
  - ❌ Щиты и броня → M8 (интерфейсы)
  - ❌ P_hit и ТТХ оружия → M9 (примитивный урон)
  - ❌ Система сканирования → M9 (нет signature_km)
  - ❌ Coupled-ассистенты → M11 (только Stabilized)
  - ❌ Асинхронные системы → M7+ (синхронная симуляция)
- ✅ Добавлены разделы о заглушках и интерфейсах
- ✅ Фокус на фундамент: FA:ON/OFF + релятивистская физика + reconciliation
- ✅ Roadmap расширений (M7-M12)

**v0.8.6 (2025-11-16):**
- Полная архитектура (избыточная для минимального подхода)

---

**Конец документа архитектуры v0.8.6 Minimal**

**Готовность:** ✅ 100%  
**Вероятность успеха:** 90%  
**Рекомендация:** 🚀 Начинать разработку с M0.1
