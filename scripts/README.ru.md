# Скрипты автоматизации серверов U2

Этот каталог содержит скрипты для автоматического запуска серверов проекта U2 для онлайн-тестирования.

## 📋 Содержание

- [Назначение](#назначение)
- [Использование](#использование)
- [Требования](#требования)
- [Порты](#порты)
- [Логи](#логи)
- [Устранение неполадок](#устранение-неполадок)
- [Ручной запуск](#ручной-запуск)

## 🎯 Назначение

Скрипты автоматизируют запуск следующих компонентов для онлайн-тестирования:

1. **C# Backend Server** - серверная часть игры
   - UDP сервер на порту 7777
   - WebSocket relay на порту 8080
   
2. **Vite Development Client** - клиентская часть
   - HTTP сервер на порту 5173

## 🚀 Использование

### Linux / macOS

```bash
# Из корня проекта
./scripts/start-servers.sh

# Или через npm
npm run start:servers
```

**Что делает скрипт:**
1. ✅ Проверяет наличие необходимых зависимостей (.NET SDK, Node.js)
2. ✅ Проверяет доступность портов 7777, 8080, 5173
3. ✅ Собирает C# проект
4. ✅ Запускает backend сервер
5. ✅ Запускает Vite client
6. ✅ Выводит информацию о подключении
7. ✅ Ожидает нажатия Ctrl+C для остановки

**Остановка серверов:**
Нажмите `Ctrl+C` в терминале, где запущен скрипт. Оба сервера будут корректно остановлены.

### Windows

```batch
# Из корня проекта
scripts\start-servers.bat
```

**Особенности Windows версии:**
- Открывает два отдельных окна консоли:
  - Одно для C# backend сервера
  - Одно для Vite client сервера
- Для остановки серверов закройте соответствующие окна или нажмите Ctrl+C в них

## 📦 Требования

### Обязательные зависимости

- **.NET 8.0 SDK** или выше
  - Проверка: `dotnet --version`
  - Скачать: https://dotnet.microsoft.com/download

- **Node.js 18** или выше
  - Проверка: `node --version`
  - Скачать: https://nodejs.org/

- **npm** (устанавливается вместе с Node.js)
  - Проверка: `npm --version`

### Установка зависимостей проекта

Перед первым запуском установите зависимости:

```bash
# Установка npm пакетов
npm install

# Сборка C# проекта (опционально, скрипт сделает это автоматически)
dotnet build U2.sln
```

## 🌐 Порты

По умолчанию используются следующие порты:

| Сервис | Протокол | Порт | URL | Описание |
|--------|----------|------|-----|----------|
| Backend UDP | UDP | 7777 | localhost:7777 | Игровой UDP сервер |
| Backend WebSocket | WebSocket | 8080 | ws://localhost:8080/ | WebSocket relay для браузерных клиентов |
| Client | HTTP | 5173 | http://localhost:5173/ | Vite dev сервер |

**Доступ к игре:**
1. Запустите скрипт `start-servers.sh` или `start-servers.bat`
2. Откройте браузер
3. Перейдите на http://localhost:5173/
4. Игра автоматически подключится к серверу через WebSocket

## 📝 Логи

### Linux / macOS

Логи сохраняются в каталоге `logs/` в корне проекта:
- Backend: `logs/backend.log`
- Client: `logs/client.log`

**Просмотр логов в реальном времени:**
```bash
# Backend лог
tail -f logs/backend.log

# Client лог
tail -f logs/client.log
```

### Windows

Логи отображаются непосредственно в окнах серверов.

## 🔧 Устранение неполадок

### Ошибка: "Порт уже занят"

**Проблема:** Один из портов (7777, 8080, 5173) занят другим процессом.

**Решение для Linux / macOS:**

```bash
# Найти процесс, занимающий порт
lsof -i :7777
lsof -i :8080
lsof -i :5173

# Или использовать netstat
netstat -tuln | grep 7777
netstat -tuln | grep 8080
netstat -tuln | grep 5173

# Остановить процесс (замените <PID> на ID процесса)
kill -9 <PID>
```

**Решение для Windows:**

```batch
# Найти процесс, занимающий порт
netstat -ano | findstr :7777
netstat -ano | findstr :8080
netstat -ano | findstr :5173

# Остановить процесс (замените <PID> на ID процесса)
taskkill /PID <PID> /F
```

### Ошибка: "dotnet CLI not found"

**Проблема:** .NET SDK не установлен или не находится в PATH.

**Решение:**
1. Установите .NET 8.0 SDK: https://dotnet.microsoft.com/download
2. Перезапустите терминал
3. Проверьте установку: `dotnet --version`

### Ошибка: "npm not found"

**Проблема:** Node.js не установлен или не находится в PATH.

**Решение:**
1. Установите Node.js (версия 18 или выше): https://nodejs.org/
2. Перезапустите терминал
3. Проверьте установку: `node --version` и `npm --version`

### Backend сервер не запускается

**Проверьте следующее:**

1. Убедитесь, что .NET 8.0 SDK установлен:
   ```bash
   dotnet --version
   ```

2. Попробуйте собрать проект вручную:
   ```bash
   dotnet build U2.sln
   ```
   Если есть ошибки компиляции, исправьте их.

3. Проверьте логи:
   ```bash
   cat logs/backend.log
   ```

4. Попробуйте запустить сервер вручную:
   ```bash
   dotnet run --project src/server/U2.Server.csproj -- --network
   ```

### Client сервер не запускается

**Проверьте следующее:**

1. Убедитесь, что зависимости установлены:
   ```bash
   npm install
   ```

2. Проверьте логи:
   ```bash
   cat logs/client.log
   ```

3. Попробуйте запустить вручную:
   ```bash
   npm run dev
   ```

### Проблемы с правами доступа (Linux/macOS)

Если скрипт не запускается из-за прав доступа:

```bash
chmod +x scripts/start-servers.sh
chmod +x scripts/test-startup.sh
```

## 🛠️ Ручной запуск

Если автоматический скрипт не работает, можно запустить серверы вручную:

### Backend (откройте первый терминал)

```bash
# Соберите проект
dotnet build U2.sln -c Release

# Запустите сервер
dotnet run --project src/server/U2.Server.csproj -c Release -- --network
```

Вы увидите сообщения:
```
U2 Server starting...
Network server running on port 7777
WebSocket relay running on ws://localhost:8080/
Press Ctrl+C to stop server...
```

### Client (откройте второй терминал)

```bash
# Запустите Vite dev сервер
npm run dev
```

Вы увидите сообщение с локальным URL:
```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

### Проверка работы

1. Откройте браузер
2. Перейдите на http://localhost:5173/
3. Клиент должен автоматически подключиться к серверу
4. Проверьте консоль браузера (F12) на наличие ошибок подключения

## 🧪 Тестирование

Для проверки работоспособности всех компонентов используйте тестовый скрипт:

```bash
./scripts/test-startup.sh
```

Этот скрипт проверяет:
- ✅ Наличие всех необходимых инструментов
- ✅ Структуру проекта
- ✅ Возможность сборки C# проекта
- ✅ Запуск backend сервера
- ✅ Доступность портов

## 📚 Дополнительная информация

### Структура проекта

```
u2/
├── scripts/
│   ├── start-servers.sh    # Скрипт запуска для Linux/macOS
│   ├── start-servers.bat   # Скрипт запуска для Windows
│   ├── test-startup.sh     # Тестовый скрипт
│   └── README.ru.md        # Эта документация
├── src/
│   ├── server/             # C# backend сервер
│   └── clients/            # Клиентские приложения
├── logs/                   # Логи серверов (создается автоматически)
└── package.json            # NPM конфигурация
```

### Переменные окружения

Для настройки URL сервера в клиенте используйте переменную окружения:

```bash
# Linux/macOS
VITE_SERVER_URL=ws://your-server:8080/ npm run dev

# Windows
set VITE_SERVER_URL=ws://your-server:8080/
npm run dev
```

### Режимы работы сервера

Backend сервер поддерживает два режима:

1. **M1 режим** (по умолчанию, без флагов):
   ```bash
   dotnet run --project src/server/U2.Server.csproj
   ```
   Выполняет один тик физики и завершается.

2. **Network режим** (используется в скриптах):
   ```bash
   dotnet run --project src/server/U2.Server.csproj -- --network
   ```
   Запускает полноценный сетевой сервер.

## 🐛 Отладка

### Включение подробных логов

Для получения более подробной информации:

```bash
# Backend - установите уровень логирования
export Logging__LogLevel__Default=Debug
dotnet run --project src/server/U2.Server.csproj -- --network

# Client - Vite автоматически выводит подробные логи
npm run dev
```

### Проверка сетевого подключения

```bash
# Проверьте, что порты слушаются
netstat -tuln | grep -E '(7777|8080|5173)'

# Или с lsof
lsof -i :7777
lsof -i :8080
lsof -i :5173

# Проверьте WebSocket подключение
curl -i -N -H "Connection: Upgrade" -H "Upgrade: websocket" http://localhost:8080/
```

## 📖 Связанная документация

- [Основной README](../README.md) - общая информация о проекте
- [M2.3 План](../M2.3-PLAN.md) - документация по сетевой архитектуре
- [Спецификации](../docs/specs/README.md) - техническая документация

## 💡 Советы

1. **Для разработки:** Запускайте серверы в отдельных терминалах вручную для лучшей видимости логов.

2. **Для тестирования:** Используйте автоматический скрипт для быстрого запуска обоих серверов.

3. **Горячая перезагрузка:** Vite поддерживает hot module replacement - изменения в клиентском коде применяются автоматически.

4. **Backend изменения:** При изменении C# кода нужно перезапустить backend сервер.

5. **Очистка логов:** Периодически очищайте каталог `logs/` для экономии места.

## 🆘 Поддержка

Если у вас возникли проблемы:

1. Проверьте раздел [Устранение неполадок](#устранение-неполадок)
2. Убедитесь, что все [Требования](#требования) выполнены
3. Проверьте логи в каталоге `logs/`
4. Откройте issue на GitHub: https://github.com/dkomlev/u2/issues

## 📝 Лицензия

Проект лицензирован под MIT License. См. LICENSE файл в корне репозитория.
